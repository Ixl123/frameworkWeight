/**
 * libraries reducer
 */
import * as types from '../actions/ActionTypes';

function libraries(state = [], action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default libraries;